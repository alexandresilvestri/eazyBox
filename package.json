{
  "name": "eazybox",
  "private": true,
  "type": "module",
  "version": "1.0.0",
  "workspaces": [
    "app/*",
    "shared"
  ],
  "scripts": {
    "dev": "bun run --filter @eazybox/web dev",
    "typecheck": "bun run --filter '*' typecheck",
    "test": "bun run --filter '*' test"
  },
  "devDependencies": {
    "typescript": "^6.0.3",
    "@types/bun": "latest"
  }
}
